name: aibox-audit
version: "0.1.0"
description: "Trace append-only (JSONL) de tous les tool calls — qui/quoi/quand/durée/erreur, flag mutating. Local-only (RGPD/compliance). Port de app-audit.ts (BoxIA)."
author: "AI Box"
hooks:
  - post_tool_call
commands:
  - aibox-audit
